kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: oauth-ingress
spec:
  rules:
  - host: oauth.192.168.64.46.nip.io
    http:
      paths:
      - backend:
          service:
            name: oauth-service
            port:
              number: 8000
        path: "/"
        pathType: ImplementationSpecific
