kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: vault
  namespace: system
spec:
  to:
    kind: Service
    name: spi-vault
    weight: 100
  port:
    targetPort: 8200
  tls:
    termination: edge
