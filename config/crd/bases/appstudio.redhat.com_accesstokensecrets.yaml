
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.6.1
  creationTimestamp: null
  name: accesstokensecrets.appstudio.redhat.com
spec:
  group: appstudio.redhat.com
  names:
    kind: AccessTokenSecret
    listKind: AccessTokenSecretList
    plural: accesstokensecrets
    singular: accesstokensecret
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: AccessTokenSecret is the Schema for the accesstokensecrets API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: AccessTokenSecretSpec defines the desired state of AccessTokenSecret
            properties:
              accessTokenId:
                description: AccessTokenId is the ID of the access token in the storage
                type: string
              serviceAccount:
                description: ServiceAccount is the service account to impersonate
                  as when retrieving the access token from the storage If the namespace
                  is not specified, the namespace of this object is assumed If the
                  name is not specified, the controller attempts to access the storage
                  using its service account
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              target:
                description: Target specifies the object to which the access token
                  should be persisted to
                properties:
                  configMap:
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        type: object
                      name:
                        type: string
                    required:
                    - name
                    type: object
                  containers:
                    properties:
                      containers:
                        items:
                          type: string
                        type: array
                      podLabels:
                        description: A label selector is a label query over a set
                          of resources. The result of matchLabels and matchExpressions
                          are ANDed. An empty label selector matches all objects.
                          A null label selector matches no objects.
                        properties:
                          matchExpressions:
                            description: matchExpressions is a list of label selector
                              requirements. The requirements are ANDed.
                            items:
                              description: A label selector requirement is a selector
                                that contains values, a key, and an operator that
                                relates the key and values.
                              properties:
                                key:
                                  description: key is the label key that the selector
                                    applies to.
                                  type: string
                                operator:
                                  description: operator represents a key's relationship
                                    to a set of values. Valid operators are In, NotIn,
                                    Exists and DoesNotExist.
                                  type: string
                                values:
                                  description: values is an array of string values.
                                    If the operator is In or NotIn, the values array
                                    must be non-empty. If the operator is Exists or
                                    DoesNotExist, the values array must be empty.
                                    This array is replaced during a strategic merge
                                    patch.
                                  items:
                                    type: string
                                  type: array
                              required:
                              - key
                              - operator
                              type: object
                            type: array
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: matchLabels is a map of {key,value} pairs.
                              A single {key,value} in the matchLabels map is equivalent
                              to an element of matchExpressions, whose key field is
                              "key", the operator is "In", and the values array contains
                              only "value". The requirements are ANDed.
                            type: object
                        type: object
                    required:
                    - containers
                    - podLabels
                    type: object
                  secret:
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        type: object
                      name:
                        type: string
                    required:
                    - name
                    type: object
                type: object
            required:
            - accessTokenId
            - serviceAccount
            - target
            type: object
          status:
            description: AccessTokenSecretStatus defines the observed state of AccessTokenSecret
            properties:
              message:
                type: string
              phase:
                type: string
              reason:
                type: string
            required:
            - message
            - phase
            - reason
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
